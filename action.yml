name: 'WordPress Plugin Tests'
description: 'Test WordPress plugins and themes.'
inputs:
  args:
    description: 'PHPUnit arguments'
    required: false
runs:
  using: 'docker'
  image: 'docker://ziodave/wordpress-tests:php7.0-phpunit4.8-wordpress4.9'
  args:
    - ${{ inputs.args }}
